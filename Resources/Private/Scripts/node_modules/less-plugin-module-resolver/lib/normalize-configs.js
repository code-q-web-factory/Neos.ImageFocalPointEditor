"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getOptions = void 0;
const path_1 = __importDefault(require("path"));
const utils_1 = require("./utils");
function isRegexString(str) {
    return str.startsWith('^') || str.endsWith('$');
}
function getOptions(options) {
    const { alias, rootDir } = options;
    return Object.keys(alias).reduce((prev, searchValue) => {
        const searchRegex = isRegexString(searchValue)
            ? new RegExp(searchValue)
            : new RegExp(`^${(0, utils_1.escapeRegex)(searchValue)}(/?.*|)$`);
        const replacement = alias[searchValue];
        if (typeof replacement === 'string') {
            return prev.concat([
                [
                    searchRegex,
                    (filename) => {
                        return path_1.default.join(rootDir || '', filename.replace(searchRegex, `${replacement}$1`));
                    },
                ],
            ]);
        }
        else {
            return prev.concat([
                [
                    searchRegex,
                    (filename) => {
                        return path_1.default.join(rootDir || '', filename.replace(searchRegex, replacement));
                    },
                ],
            ]);
        }
    }, []);
}
exports.getOptions = getOptions;
