import Less from 'less';
import { LessPluginModuleResolverConfigs } from './interface';
export declare class AliasFileManager extends Less.FileManager {
    readonly options: LessPluginModuleResolverConfigs;
    private matchers;
    constructor(options: LessPluginModuleResolverConfigs);
    supports(filename: string): boolean;
    supportsSync(filename: string): boolean;
    loadFile(filename: string, currentDirectory: string, options: Less.LoadFileOptions, environment: Less.Environment): Promise<Less.FileLoadResult>;
    loadFileSync(filename: string, currentDirectory: string, options: Less.LoadFileOptions, environment: Less.Environment): Less.FileLoadResult | Less.FileLoadError;
    private matchAndReplace;
}
