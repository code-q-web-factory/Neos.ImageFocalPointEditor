/// <reference types="less" />
import { LessPluginModuleResolverConfigs } from './interface';
export declare class LessPluginModuleResolver implements Less.Plugin {
    private readonly options;
    constructor(options: LessPluginModuleResolverConfigs);
    install(less: LessStatic, pluginManager: Less.PluginManager): void;
    minVersion: [number, number, number];
}
export * from './interface';
