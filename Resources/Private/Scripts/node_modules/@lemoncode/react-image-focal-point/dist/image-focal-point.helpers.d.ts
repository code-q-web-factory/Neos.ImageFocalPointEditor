interface Props {
    setX: (x: number) => void;
    setY: (y: number) => void;
    container: HTMLDivElement;
    canMove: boolean;
    onChange: (x: number, y: number) => void;
}
/**
 * This function is used to handle the mouse down event inside the image focal point
 * container, it takes the mouse position in pixels and parses it to percentages.
 */
export declare const onMove: (props: Props) => (e: any) => void;
export {};
//# sourceMappingURL=image-focal-point.helpers.d.ts.map